# reading 03

## 1. ***Version control*** is a system that records changes and lets you revist various of files or set of files

---

### 2 ***cloning*** when you clone a file you have copied all version of all files for a project.

---

#### 3 ***what is the command to track and stage files?*** the command for a single file is "git add filename" for all files in a repository you use " $ git add *

 ---

##### 4 ***what is the command to take a snapshot of your changed files?*** the command is " $ git commit -a"

---

###### 5 ***what is the command to send your changed files to github?***the command would be "$ git push [remote-name][branch-name]"
